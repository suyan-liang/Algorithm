```cpp
int a[maxn],n,num;
int main()
{
    num=0;
    cin>>n;
    for(int i=2;i*i<=n;i++){
        if( n%i==0 ) a[num++]=i; //从2开始，如果能模尽，肯定是
        while( n%i==0 ) n/=i; //疯狂除
    }
    if( n>1 ) a[num++]=n; //结果如果大于1的话还要把自己加进来
    for(int i=0;i<num;i++) cout<<a[i]<<'\n';
    return 0;
}
```

